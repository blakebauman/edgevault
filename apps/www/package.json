{
  "name": "@edgevault/www",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 8807",
    "build": "astro build && wrangler deploy --dry-run",
    "preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "lint": "biome check src"
  },
  "devDependencies": {
    "@edgevault/ui": "workspace:*",
    "astro": "^7.2.0",
    "wrangler": "catalog:"
  }
}
