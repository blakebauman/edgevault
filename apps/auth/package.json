{
  "name": "@edgevault/auth-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8802 --inspector-port 9301",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@edgevault/auth": "workspace:*",
    "@edgevault/crypto": "workspace:*",
    "@edgevault/database": "workspace:*",
    "@edgevault/edge-protocol": "workspace:*",
    "@edgevault/sso-saml": "workspace:*",
    "@hono/zod-validator": "catalog:",
    "@simplewebauthn/server": "^13.3.1",
    "drizzle-orm": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@edgevault/typescript-config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
