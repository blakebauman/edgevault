{
  "name": "@edgevault/auth-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@edgevault/auth": "workspace:*",
    "@edgevault/crypto": "workspace:*",
    "@edgevault/database": "workspace:*",
    "@edgevault/edge-protocol": "workspace:*",
    "@edgevault/sso-saml": "workspace:*",
    "@hono/zod-validator": "^0.7.0",
    "@simplewebauthn/server": "^13.3.1",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.9.2",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@edgevault/typescript-config": "workspace:*",
    "typescript": "^5.9.2",
    "vitest": "^4.1.8",
    "wrangler": "^4.98.0"
  }
}
