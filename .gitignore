# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.turbo/
.wrangler/
*.tsbuildinfo

# Generated Cloudflare types
worker-configuration.d.ts

# React Router typegen output (regenerated by `react-router typegen`, which
# every console typecheck/build already runs)
.react-router/

# Env / secrets
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example
!.env.example

# Coverage
coverage/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Astro generated artifacts
.astro/

# Impeccable session state. design.json + .impeccable.md ARE committed — they're
# the design source of record (see CLAUDE.md); live/ and critique/ are per-run
# working notes and are not.
.impeccable/live/
.impeccable/critique/

# Console redesign prototype — scratch navigable preview, not shipped code
apps/console/prototype/

# Claude Code local session settings
.claude/settings.local.json

# Internal sales/strategy material — names competitors, must not ship in the open-core repo
docs/internal/
